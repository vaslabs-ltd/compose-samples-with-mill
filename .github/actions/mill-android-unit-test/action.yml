inputs:
  app-name:
    description: 'The name of the Android app to test, e.g., JetNews or JetLagged'
    required: true

runs:
  using: "composite"
  steps:
    - name: Run instrumentation tests
      run: |
        ./mill show ${{ inputs.app-name }}.test
      shell: bash